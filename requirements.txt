# InsightLens 依赖
# 最小依赖集 — 仅网页解析和 HTTP 请求
# 核心提取引擎只需 Python 标准库 + 以下两个包

# 必须
beautifulsoup4>=4.9.0     # HTML 解析
lxml>=4.6.0               # 快速 XML/HTML 解析器

# 推荐
httpx>=0.24.0             # 异步 HTTP 客户端（用于搜索和订阅）
